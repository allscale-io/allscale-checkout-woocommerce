# Screenshots

Rendered from the real `assets/css/admin.css` + brand assets via Chrome headless so they stay 1:1 with what the plugin actually outputs. Re-render after changing the CSS or any of the rendered HTML.

| File | Used by | What it shows |
|---|---|---|
| `wc-required-notice.png` | `README.md` — "Requires WooCommerce" | The branded notice shown when the plugin activates on a site without WooCommerce |
| `settings-healthy.png` | `README.md` — "Setup" | The gateway settings page in its healthy steady state — credentials saved, connection verified, webhook receiving |
| `wizard-webhook.png` | `README.md` — "Installation" / wizard | Step 3 of the 4-step setup wizard — the unique "you must paste this URL into Allscale yourself" moment |
| `order-meta-box.png` | `README.md` — "Order statuses" | The Allscale Payment meta box on a confirmed order, showing tx hash, paid/fee/net breakdown, chain, intent id |

When you add a new screenshot, put it here and reference it from the doc that needs it. Use a stable, descriptive filename (kebab-case, .png preferred) so updates don't break links.
